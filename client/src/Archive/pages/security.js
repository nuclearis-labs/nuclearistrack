// security.js
import React from 'react';
import styled from 'styled-components';
import { Title, Wrap } from '../components/components.js';
import { NavPhraseFixed, WebTop, WidthContent, WebTopTit, ItemTit, LongTxt, BottomSpace } from '../components/webComponents.js';
import bg from '../img/bgSeguridad.jpg';

const WebTopSecurity = styled(WebTop)`
  height:396px;
  background: url(${bg}) #000 no-repeat center;
`;

const GoldTitle = styled(Title)`
  color:#8c6239;
`;

function Security() {
  return (
    
  );
}
export default Security;